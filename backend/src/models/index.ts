import Store, { IStore } from "../models/store.model";
import Discount, { IDiscount } from "../models/discount.model";

import Product, { IProduct } from "./product.model";
import Reel, { IReel } from "./reel.model";

export { Discount, Product, Store, Reel };

export { IDiscount, IProduct, IStore, IReel };
